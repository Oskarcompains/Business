import React from 'react';
import {Building2,Users2,ShieldCheck} from 'lucide-react';
import {useApp,ActiveView} from '../../context/AppContext';
import {useSecureAuth} from '../../context/SecureAuthContext';
export const Sidebar:React.FC=()=>{const {activeView,setActiveView}=useApp();const {user}=useSecureAuth();const admin=!!user&&['SUPERADMIN','ADMIN'].includes(user.role);const items:{id:ActiveView;label:string;icon:React.ReactNode;admin?:boolean}[]=[{id:'companies',label:'Empresas',icon:<Building2 className="w-4 h-4"/>},{id:'admin',label:'Administración',icon:<ShieldCheck className="w-4 h-4"/>,admin:true},{id:'users',label:'Usuarios',icon:<Users2 className="w-4 h-4"/>,admin:true}];return <aside className="hidden md:flex flex-col w-64 border-r border-slate-800 p-4 bg-[#0a1329]/70"><p className="px-3 mb-3 text-[10px] font-bold tracking-[.2em] text-slate-500">GESTIÓN</p><div className="space-y-1">{items.filter(i=>!i.admin||admin).map(i=><button key={i.id} onClick={()=>setActiveView(i.id)} className={`w-full flex items-center gap-3 px-3 py-2.5 rounded-xl text-sm ${activeView===i.id?'bg-red-600/20 border border-red-500/30 text-white':'text-slate-400 hover:text-white hover:bg-slate-800/50'}`}>{i.icon}{i.label}</button>)}</div><div className="mt-auto p-3 text-xs text-slate-500 border-t border-slate-800">Datos reales · Sin contenido demo</div></aside>};
