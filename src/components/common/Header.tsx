import React from 'react';
import {ShieldCheck,LogOut} from 'lucide-react';
import {useSecureAuth} from '../../context/SecureAuthContext';
export const Header:React.FC=()=>{const {user,logout}=useSecureAuth();return <header className="sticky top-0 z-40 border-b border-slate-800 bg-[#070d1e]/95 backdrop-blur"><div className="max-w-7xl mx-auto h-16 px-4 sm:px-6 flex items-center justify-between"><div><div className="font-black tracking-tight">IBARBASO <span className="text-red-500">BUSINESS CLUB</span></div><div className="text-[10px] uppercase tracking-[.22em] text-slate-500">Área privada</div></div><div className="flex items-center gap-3"><div className="hidden sm:block text-right"><div className="text-sm font-bold">{user?.displayName||user?.email}</div><div className="text-[11px] text-slate-500">{user?.role}</div></div>{user?.role==='SUPERADMIN'&&<ShieldCheck className="w-5 h-5 text-red-500"/>}<button onClick={logout} title="Cerrar sesión" className="p-2 rounded-lg border border-slate-800 text-slate-400 hover:text-white"><LogOut className="w-4 h-4"/></button></div></div></header>};
