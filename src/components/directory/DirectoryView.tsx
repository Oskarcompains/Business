import React,{useEffect,useState} from 'react';
import {Building2,RefreshCw} from 'lucide-react';
import {api} from '../../services/api';
export const DirectoryView:React.FC=()=>{const [companies,setCompanies]=useState<any[]>([]);const [loading,setLoading]=useState(true);const [error,setError]=useState('');const load=async()=>{setLoading(true);setError('');try{const r=await api.companies();setCompanies(r.companies||[])}catch(e:any){setError(e.message||'No se pudieron cargar las empresas')}finally{setLoading(false)}};useEffect(()=>{load()},[]);return <div className="space-y-5"><div className="flex items-center justify-between"><div><p className="text-xs font-black tracking-widest text-red-500">DIRECTORIO REAL</p><h1 className="text-3xl font-black">Empresas</h1><p className="text-slate-400 text-sm">Solo aparecen empresas registradas en la base de datos.</p></div><button onClick={load} className="p-2 border border-slate-800 rounded-xl text-slate-400"><RefreshCw className="w-4 h-4"/></button></div>{error&&<div className="p-4 rounded-xl border border-red-800 bg-red-950/30 text-red-300">{error}</div>}{loading?<div className="text-slate-500 py-10">Cargando…</div>:companies.length===0?<div className="rounded-2xl border border-slate-800 bg-[#0b152d] p-10 text-center"><Building2 className="w-10 h-10 mx-auto text-slate-600 mb-3"/><h2 className="font-bold">Todavía no hay empresas</h2><p className="text-sm text-slate-500 mt-1">Cuando Kroko apruebe una solicitud, la empresa aparecerá aquí.</p></div>:<div className="grid sm:grid-cols-2 xl:grid-cols-3 gap-4">{companies.map(c=><div key={c.id} className="rounded-2xl border border-[#1b3164] bg-[#0b152d] p-5"><div className="flex items-start justify-between gap-3"><div><h2 className="font-bold text-lg">{c.name}</h2><p className="text-sm text-slate-400">{c.sector||'Sector sin indicar'}</p></div>{c.membership_tier&&<span className="text-[10px] font-black px-2 py-1 rounded-full border border-red-500/30 text-red-300">{c.membership_tier}</span>}</div>{c.website&&<a href={c.website} target="_blank" rel="noreferrer" className="inline-block mt-4 text-xs text-red-400">Visitar web</a>}</div>)}</div>}</div>};
